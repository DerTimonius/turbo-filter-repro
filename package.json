{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "echo \"hello world\"",
    "start": "turbo run dev --filter=!docs",
    "start.docs": "turbo run dev --filter=docs",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-svelte": "^3.2.2",
    "turbo": "^2.0.6"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "name": "with-svelte"
}
